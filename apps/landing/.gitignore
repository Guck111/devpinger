dist/
.astro/
node_modules/

# pnpm is the source of truth; ignore npm/yarn locks that the Cloudflare
# Workers GitHub App auto-generates.
package-lock.json
yarn.lock

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
